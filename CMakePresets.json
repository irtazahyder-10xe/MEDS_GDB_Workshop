{
  "version": 4,
  "configurePresets": [
    {
      "name": "host",
      "displayName": "Host Preset",
      "description": "Preset to builds examples for host architecture",
      "binaryDir": "${sourceDir}/build",
      "cacheVariables": {
        "SPIKE": "OFF"
      }
    },
    {
      "name": "spike",
      "displayName": "Spike Preset",
      "description": "Preset to builds examples to run on Spike",
      "binaryDir": "${sourceDir}/spike.build",
      "cacheVariables": {
        "CMAKE_TOOLCHAIN_FILE": "TC-riscv.cmake",
        "SPIKE": "ON"
      }
    }
  ]
}
